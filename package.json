{
  "name": "set50-relay",
  "version": "1.0.0",
  "private": true,
  "description": "Intraday state drop-box for the SET50 Options dashboard (multi-PC). No deps.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
